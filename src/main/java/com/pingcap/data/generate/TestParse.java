package com.pingcap.data.generate;

//import net.sf.jsqlparser.util.TablesNamesFinder;
//
//import java.util.Set;

public class TestParse {
    public static void main(String[] args) throws Exception {
//        String sql = "SELECT LOW_PRIORITY  job_id,  table_id,  scan_id,  scan_range_start,  scan_range_end,  expire_time,  owner_id,  owner_addr,  owner_hb_time,  status,  status_update_time,  state,  created_time FROM mysql.tidb_ttl_task WHERE status = 'waiting' OR (owner_hb_time < '2024-08-01 09:00:30' AND status = 'running') ORDER BY created_time ASC;";
//        Set<String> tableNames = TablesNamesFinder.findTables(sql);
//        System.out.println(tableNames);
        int i = 1;
        System.out.println(i++);
    }
}
